# DebateArena Integrated — Top-level requirements
# Run from d:\debate-arena-integrated\
#
# Only the orchestrator runs locally. Heavy ML packages (torch, transformers,
# wav2vec2, Kokoro) stay on SLURM inside the Emotion API.
#
# Usage:
#   python -m venv venv-orchestrator
#   .\venv-orchestrator\Scripts\pip install -r requirements.txt

fastapi>=0.111
uvicorn[standard]>=0.29
httpx>=0.27
pydantic>=2.0
websockets>=12.0
