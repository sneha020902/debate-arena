# --- API server ---
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9      # required by FastAPI for multipart file uploads
pydantic>=2.6.0

# --- Text emotion ---
transformers>=4.36.0
torch>=2.1.0
safetensors>=0.4.0
huggingface-hub>=0.20.0

# --- Speech emotion + prosody ---
torchaudio>=2.1.0
librosa>=0.10.0
soundfile>=0.12.0
scipy>=1.11.0
numpy>=1.24.0

# --- Voice-to-text (transcription) ---
faster-whisper>=1.0.0

# --- Text-to-voice (synthesis); needs network access at request time ---
edge-tts>=6.1.0

# --- ElevenLabs (optional — only needed for the dormant provider="elevenlabs"
# path in synthesis.py; app runs fine without it on the default edge-tts path) ---
# elevenlabs>=1.0.0

# --- Thin Streamlit client (demo_app.py only) ---
streamlit>=1.30.0
plotly>=5.18.0
pandas>=2.0.0
requests>=2.31.0